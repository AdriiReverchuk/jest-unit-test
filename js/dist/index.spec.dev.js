"use strict";

var _index = require("./index.js");

describe("Add Item Form", function () {
  it("should create", function () {
    expect.assertions(1);
    var form = new _index.AddItemForm();
    expect(form).toBeTruthy();
  });
  it("should generate proper DOM", function () {});
});